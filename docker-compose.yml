services:
  payloadprobe:
    build: .
    container_name: payloadprobe-lab
    environment:
      PAYLOADPROBE_STORE_PATH: /data/xmlResponses.json
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
