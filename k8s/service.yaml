apiVersion: v1
kind: Service
metadata:
  name: payloadprobe
  namespace: payloadprobe
  labels:
    app: payloadprobe
spec:
  selector:
    app: payloadprobe
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
